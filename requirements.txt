# Exact runtime dependency lock validated for the v1.0.1 release.
# Refresh intentionally and rerun the full verifier before publishing changes.
certifi==2026.6.17
cffi==2.1.0
charset-normalizer==3.4.9
cryptography==50.0.0
idna==3.18
pycparser==3.0
requests==2.34.2
urllib3==2.7.0
